const sinon = require('sinon');
const assert = require('assert');

const functionTest = (cb) => {
	const num = 3;
	cb(3)
}

const prueba = () => {

}

it("test should call all subscribers when exceptions:", function () {
});

